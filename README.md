# Game Framework

This is a simple dice game made with Java.

The project uses the Template Method design pattern. The `Game` class defines the basic structure of the game and `DiceGame` implements the game rules.

## How it works

Players take turns rolling a dice. The score is added after each turn. The first player to reach 20 points wins.

## Technologies

- Java
- Maven
- IntelliJ IDEA